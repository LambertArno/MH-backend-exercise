{
  "name": "GraphQL",
  "version": "1.0.0",
  "description": "Create a GraphQL endpoint that reads information from a commit on GitHub and exposes all fields returned by it.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Arno Lambert",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "express-graphql": "^0.12.0",
    "graphql": "^15.5.0"
    "axios": "^0.21.1",
    "nodemon": "^2.0.7"
  }
}
